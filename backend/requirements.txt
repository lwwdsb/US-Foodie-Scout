# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Data Validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# LangChain Agent (flexible — let pip resolve compatible set)
langchain>=0.3.13
langchain-core>=1.0.0
langchain-openai>=0.3.0

# Redis
redis>=5.2.0

# HTTP Client
httpx>=0.28.0

# SSE Streaming
sse-starlette>=2.1.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.20

# Tavily — web search fallback for XHS sentiment (activate with TAVILY_API_KEY in .env)
tavily-python>=0.5.0

# XHS unofficial API wrapper intentionally removed — causes account bans (see memory).
